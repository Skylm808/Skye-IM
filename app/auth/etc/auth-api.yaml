Name: auth-api
Host: 127.0.0.1
Port: 10001

# Etcd 服务注册（让Gateway可以发现此服务）
Etcd:
  Hosts:
    - 127.0.0.1:2379
  Key: auth-api

# User RPC 服务配置
UserRpc:
  Etcd:
    Hosts:
      - 127.0.0.1:2379
    Key: user.rpc

# MySQL 配置（临时保留）
MySQL:
  DataSource: root:630630@tcp(127.0.0.1:3306)/im_auth?charset=utf8mb4&parseTime=True&loc=Local

# Redis 缓存配置（用于Model缓存，临时保留）
Cache:
  - Host: 127.0.0.1:16379
    Type: node
    Pass: ""

# Redis 配置（用于验证码等）
Redis:
  Host: 127.0.0.1:16379
  Type: node
  Pass: ""

# JWT AccessToken 配置
Auth:
  AccessSecret: "Skylm-im-secret-key"
  AccessExpire: 604800  # 7天（秒）

# RefreshToken 配置
RefreshToken:
  Secret: "Skylm-im-refresh-secret-key"
  Expire: 2592000  # 30天（秒）

# QQ邮箱配置
Email:
  Host: smtp.qq.com
  Port: 465
  Username: 1787924335@qq.com
  Password: foocrtqncvglbjji
  From: "Skye-IM系统"

# 验证码配置
Captcha:
  Expire: 300  # 5分钟过期
  Length: 6    # 6位验证码

# 日志配置
Log:
  ServiceName: auth-api
  Mode: console
  Level: info
