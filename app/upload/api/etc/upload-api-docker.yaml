Name: upload-api
Host: 0.0.0.0
Port: 10600

# JWT配置（与Auth服务保持一致）
Auth:
  AccessSecret: "Skylm-im-secret-key"
  AccessExpire: 604800  # 7天（秒）

# MinIO配置
MinIO:
  Endpoint: "minio:9000"              # 容器内部访问地址
  PublicEndpoint: "localhost:9000"    # 前端浏览器访问地址
  AccessKeyID: "minioadmin"
  SecretAccessKey: "minioadmin"
  UseSSL: false
  Buckets:
    Avatar: "avatars"      # 头像
    Image: "images"        # 聊天图片
    File: "files"          # 文件
    Video: "videos"        # 视频
    Voice: "voices"        # 语音

# 文件上传限制
Upload:
  MaxImageSize: 10485760      # 10MB
  MaxFileSize: 104857600      # 100MB
  MaxAvatarSize: 5242880      # 5MB
  AllowedImageTypes: ["image/jpeg", "image/png", "image/gif", "image/webp"]
  AllowedFileTypes: ["application/pdf", "application/msword", "application/vnd.openxmlformats-officedocument.wordprocessingml.document", "application/zip"]

# 日志配置
Log:
  ServiceName: upload-api
  Mode: console
  Level: info
